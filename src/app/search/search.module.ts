import {NgModule} from '@angular/core';
import {SearchService} from './search.service';

@NgModule({
    declarations: [
    ],
    providers: [
        SearchService,
    ],
})

export class SearchModule { }
