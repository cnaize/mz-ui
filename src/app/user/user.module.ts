import {NgModule} from '@angular/core';
import {UserService} from './user.service';

@NgModule({
    declarations: [
    ],
    providers: [
        UserService,
    ],
})

export class UserModule { }
